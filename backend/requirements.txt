# Runtime dependencies.
#
# PyJHora is installed with --no-deps in the Dockerfile, because it declares
# PyQt6 for its desktop UI module. This application never imports jhora.ui, so
# pulling in Qt would add roughly 100 MB to the image for nothing. Its real
# runtime needs are listed here instead: swisseph, geopy and pytz.
PyJHora==4.8.7
pyswisseph==2.10.3.2
geopy>=2.4
pytz>=2024.1

fastapi>=0.115
uvicorn[standard]>=0.30
pydantic>=2.9
requests>=2.32
timezonefinder>=6.5
numpy>=1.26
